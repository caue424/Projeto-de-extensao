/*package test;

import model.Estagiario;
import org.junit.jupiter.api.Test;
import service.EstagioService;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class EstagiarioTest {

    @Test
    void deveAdicionarTicket()

    {
        Estagiario estagiario = new Estagiario("João");
        EstagioService service = new EstagioService();

        service.adicionarTicket(estagiario);

        assertEquals(1, estagiario.getTickets());
    }

    @Test
    void deveCalcularProdutividade() {
        Estagiario estagiario = new Estagiario("Maria");
        EstagioService service = new EstagioService();

        for (int i = 0; i < 3; i++) {
            service.adicionarTicket(estagiario);
        }

        double produtividade = service.calcularProdutividade(estagiario);
        assertEquals(60.0, produtividade);
    }


}
*/




















